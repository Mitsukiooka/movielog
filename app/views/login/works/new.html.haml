%section#contact.contact.section-show
  .container
    .section-title
      %h2 New Movie
      %p New Movie
    =render 'form'
    =link_to "Back to index", works_path(anchor: 'portfolio'), class: 'btn submitbtn'
